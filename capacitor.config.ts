import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.missionaria.canaa',
  appName: 'Assembleia de Deus Missionária Canaã',
  webDir: 'dist'
};

export default config;
